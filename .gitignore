node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.local/

# Local secrets - never commit
.env
.env.local
# Isolated E2E test environment (Razorpay TEST keys + Neon branch). Never committed.
.env.test
